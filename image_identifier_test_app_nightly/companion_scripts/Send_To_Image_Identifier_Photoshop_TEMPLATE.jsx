/* Template only.
   In the app, use Advanced > External Workflow > Install / Refresh Companion Scripts
   to generate a Photoshop JSX script with your current watch folder path. */
#target photoshop
alert("Generate the configured script from Image Identifier first.");
